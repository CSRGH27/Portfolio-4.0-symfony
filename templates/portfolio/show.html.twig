

{% block body %}
{# <div class="box">
    <h2>{{ project.title }}</h2>
    <img src="{{ project.image }}" alt="">
    <div>{{ project.content }}</div>
    {% for languages in project.languages %} <img src="{{ languages.image }}" alt=""> {% endfor %}
    

</div> #}


            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="exampleModalLabel">{{ project.title }}</h4>
            </div>
            <div class="modal-body">
                
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default closed" data-dismiss="modal">Close</button>
            </div>
        





{% endblock %}